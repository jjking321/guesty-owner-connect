

# Reduce Refresh Rate Limit Cooldown

## File: `src/components/TaxExemptTable.tsx`

Change the cooldown from 10,000ms to 2,000ms (2 seconds), and update the toast message accordingly.

**Line ~25**: Change `10000` to `2000`
**Line ~26**: Update toast message from "10 seconds" to "a couple seconds"

Single-line changes, no other files affected.

